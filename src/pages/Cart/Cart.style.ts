import { createUseStyles } from "react-jss";

export default createUseStyles({
  cart: {
    minWidth: 300,
    width: "90%",
    maxWidth: 900,
    maxHeight: "80vh",
    margin: "0 auto",

    borderRadius: 10,
    borderCollapse: "collapse",
    background: "#fff",
  },
});
